import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.flashcards.art',
  appName: 'flash-cards',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
